import { SEARCH_TAG, KEY_ENTER_UP } from "../actions";

const searches = (state = [], action) => {

  switch (action.type) {
    case SEARCH_TAG:

      {
        return state
      }
    case KEY_ENTER_UP:
      {
        return state
      }
    default:
      return state
  }
}

export default searches